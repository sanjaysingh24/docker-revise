apiVersion: v1
kind: Pod

metadata:
  name: my-first-pod
  labels:
    app: demo
spec:
  containers:
    - name: node-app
      image: node:24
      command: ["node", "-e", "console.log('Hello from Node.js Pod!')"]